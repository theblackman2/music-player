import styled from "styled-components";
import Layout from "../../Layout";

function Artists() {
  const page = (
    <div className="artisestes">
      Artistes
    </div>
  );
  return (
    <Layout page = {page} />
  )
}

export default Artists